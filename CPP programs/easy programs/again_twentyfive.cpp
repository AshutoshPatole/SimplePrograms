/// problem link =>
#include <iostream>
#include <math.h>
using namespace std;
int main()
{

    cin.tie(NULL);
    ios_base::sync_with_stdio(false);
    int power;
    cin >> power;
    // string ans;
    // long long int res = pow(5, power);
    // cout<<res<<endl;
    // ans = to_string(res);
    // cout << ans[ans.size() - 2] << ans[ans.size() - 1];
    cout << 25;
    return 0;
}